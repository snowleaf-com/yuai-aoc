import styles from './SectionBubbleBottom.module.css';
const SectionBubbleBottom: React.FC = () => {
  return (
    <div className={styles.sectionBubbleBottom}>
    </div>
  );
};

export default SectionBubbleBottom;
